{
    "all": {
        "tasks":[{
            "$function": "console.print",
            "$args": [ "DOC @@@@@@@@@@@@@@@@@@ ", "{$change.doc}" ]
        }]
    },
    "sample": {
                        "$every": "{#every.item}",
                        "$collect": "result2",
                        "$tasks":[{
                            "$function": "JSON.parse",
                            "$args": [ "{#every.item}" ],
                            "$set": "data.jsonItem"
                        },{
                            "$function": "console.print",
                            "$args": [ "Dataaaa Inner: ", "{#data.jsonItem}" ]
                        }],
                        "$set": "result3"
                    }
}